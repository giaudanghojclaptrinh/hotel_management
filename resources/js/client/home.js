// HOME PAGE SPECIFIC JAVASCRIPT
// Layout (header/footer/navigation) JS loaded from layout.js

document.addEventListener('DOMContentLoaded', () => {
    console.log('Home Page JS Loaded');

    // Thêm các tính năng đặc thù cho trang home ở đây nếu cần
    // Ví dụ: slider, animations, etc.
});
